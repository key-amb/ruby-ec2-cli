#!/usr/bin/env ruby

require 'aws-sdk-core'

require 'ec2-cli'

commands = %w|list start stop|
command  = nil
if commands.include?(ARGV[0])
  command = ARGV.shift
end
command ||= commands[0]

ec2 = Aws::EC2::Client.new

EC2Cli.send(command, ec2)

